# SPDX-FileCopyrightText: Copyright (c) 2023 - 2026 NVIDIA CORPORATION & AFFILIATES.
# SPDX-FileCopyrightText: All rights reserved.
# SPDX-FileCopyrightText: Copyright (c) 2026 The University of Chicago.
# SPDX-License-Identifier: Apache-2.0
#
# AMIP RSI forecaster, ERDM_fancy contract — the RSI-vs-ERDM comparison pair.
#
# A byte-copy of model/amip_erdm_fancy.yaml with exactly one delta:
# ``rolling_dit_kwargs.output_head.num_output_heads: 2``. It exists so RSI can
# be compared against the one REAL v2-trained checkpoint we hold
# (``ERDM_ERDM_fancy_42_2026-08-10T13-21-13``, translated to
# ``erdm_fancy.mdlus``) on a like-for-like contract: 154 channels, c_grid_dim 6,
# scalar_dim 3, nocean 3, SST-anomaly channel and global_mean_sst trend scalar.
#
# ``amip_rsi_v2`` (151 ch, CO2-scalar, no ocean) cannot be compared against that
# checkpoint — different channel count, different c_grid, different scalar. Use
# THIS config whenever the baseline is erdm_fancy, and amip_rsi_v2 for a
# from-scratch ERDM v2 pair.
#
# Requires the SST suite on the DATASET side, exactly as erdm_fancy does:
#     dataset.sst_anomaly_channel=append
#     dataset.scalar_forcing=global_mean_sst
#     dataset.sst_climatology_path=<norm_stats/sst_climatology.npz>
# The model config alone cannot switch it on — the anomaly channel is derived by
# the pipeline's rescaler, not stored.
#
# Original ERDM_fancy header follows.
# ---------------------------------------------------------------------------
# AMIP ERDM forecaster, ERDM_fancy variant — amip_v2 (Phase 12h).
#
# Mirrors upstream ``configs/ERDM_fancy.yaml``, and specifically the contract of
# the FIRST REAL v2-TRAINED CHECKPOINT we have:
#   ERDM_ERDM_fancy_42_2026-08-10T13-21-13/last.ckpt  (project: amip_v2)
#
# Every width below is DERIVED from the variable lists, and all four agree with
# the numbers that checkpoint's own config.yml states by hand:
#     in/out_channels 154   c_grid_dim 6   scalar_dim 3   nocean 3
# with ocean_grid_indices [1, 2, 3] — an independent cross-check of the 12b
# packing, the 12e feature set, 12f's ocean tail and 12g's anomaly channel.
#
# Differences from amip_erdm_v2_ocean.yaml, all from the SST suite being on:
#   * ``sea_surface_temperature_anomaly`` is listed in varying_boundary_variables.
#     It is NOT stored — 12g's rescaler derives it and inserts it right after the
#     absolute SST channel — but it IS a channel the model sees, so it has to be
#     listed here or c_grid_dim comes out one short. This is the post-rescaler
#     order (``SSTForcing.grid_forcing_names``).
#   * ``global_mean_co2`` is GONE from the varying list and nothing is
#     scalar-routed: the trend scalar is the ocean-mean SST anomaly
#     (``dataset.scalar_forcing: global_mean_sst``), which occupies the same
#     third calendar slot CO2 would have. scalar_dim stays 3.
#   * nocean 3, not 2: the anomaly is itself a predicted ocean channel.
#
# Pair with a dataset config carrying
#     sst_anomaly_channel: append
#     scalar_forcing: global_mean_sst
#     sst_climatology_path: <norm_stats/sst_climatology.npz>
# (see conf/dataset/amip_dailyavg_coarse.yaml's SST block) — the model config
# alone cannot switch the suite on; the derived channel comes from the pipeline.
#
# Identical to ``amip_erdm_v2.yaml`` except ``ocean_state_variables``: SST and
# sea-ice cover become PREDICTED channels as well as forcings, so
# in/out_channels go 151 -> 153. The scheduler supervises them against the
# boundary at each frame's own time and re-imposes the truth at the top of
# every roll, so the predictions never feed back into the forecast — they are
# a diagnostic of whether the model has internalised the ocean state.
#
# Warm-start from a trained ``amip_erdm_v2`` run with
# ``training.partial_checkpoint=<path>.mdlus``: ocean support only ADDS
# parameters (``input_embed.ocean_embed``, the head's ``ocean_experts`` /
# ``ocean_gate``), so the expected report is zero skipped keys.
#
# Original header follows.
#
# AMIP ERDM forecaster — amip_v2 rebaseline (Phase 12a.3).
#
# Mirrors upstream amip_v2 ``configs/ERDM_co2.yaml`` @ ``e0b7b60``:
# :class:`RollingDiTWrapper` (v2's forecaster backbone — upstream deleted
# the ERDMUnet variant that ``model/amip_erdm.yaml`` wraps) at the
# upstream size (dim 1024, 20 blocks, 16/8 heads) with the daily-avg
# ERA5 variable set (6 surface / 5 upper-air x 26 levels / 15 diagnostic
# = 151 state channels) on the 45x90 coarse forecaster grid.
#
# Pairs with ``dataset=amip_dailyavg_coarse`` (Phase 12c, converted
# 1978-2022) + ``loss=erdm_v2``. Coarse 45x90 state, 1-degree forcings
# reduced by this model's stride-4 conv — upstream ERDM_co2.yaml's layout.
#
# Phase 12e landed the full upstream feature set below (budget input
# projection, mixture output head, forcing cross-attention, global
# conditioning), so this config now mirrors upstream ERDM_co2.yaml's model
# block. ``state_layout`` is NOT listed: the wrapper derives the state block
# sizes from its own variable lists.

name: RollingDiTWrapper
module: physicsnemo.experimental.models.amip_si

# Model timestep, in hours (2026-08-13 stride audit). The step belongs to the
# MODEL family, not the store: the same 6-hourly archive can feed a 24-hour and
# a 6-hour model. Source of truth for this one: amip_v2 ERDM_ocean.yaml
# (every amip config, v1 and v2, steps 24 h).
# `train_loop.model_step_rows` divides it by the store's own
# `data_timedelta_hours` to get the row stride, and raises if the dataset
# config's `forecast_lead_times` disagrees.
timedelta_hours: 24

surface_variables:
  - skin_temperature
  - surface_pressure
  - 2m_temperature
  - 2m_specific_humidity
  - 10m_u_component_of_wind
  - 10m_v_component_of_wind
upper_air_variables:
  - temperature
  - u_component_of_wind
  - v_component_of_wind
  - geopotential
  - specific_humidity
diagnostic_variables:
  - USWRFtoa_24h
  - ULWRFtoa_24h
  - USWRFsfc_24h
  - ULWRFsfc_24h
  - DSWRFsfc_24h
  - DLWRFsfc_24h
  - LHTFLsfc_24h
  - SHTFLsfc_24h
  - PRATEsfc_24h
  - hcc_24h
  - lcc_24h
  - mcc_24h
  - mn2t_24h
  - mx2t_24h
  - mxtpr_24h
constant_boundary_variables:
  - geopotential_at_surface
  - land_sea_mask
# Post-rescaler order: the anomaly is derived, not stored (see the header).
varying_boundary_variables:
  - DSWRFtoa_24h_lead
  - sea_surface_temperature_monthly_interp
  - sea_surface_temperature_anomaly
  - sea_ice_cover_monthly_interp

levels: [5, 7, 10, 20, 30, 50, 70, 100, 125, 150, 175, 200, 250, 300, 400,
         500, 600, 700, 800, 850, 875, 900, 925, 950, 975, 1000]
# The v2 forecaster's coarse grid (180x360 pre-coarsened x4, Phase 12c).
horizontal_resolution: [45, 90]
# Phase 12d: CO₂ is a spatially uniform map in the store, so the
# ForcingAssembler pops it out of the gridded stream and appends it to the
# calendar row — upstream's ``forcing_from_raw`` step 3. This gives exactly
# upstream ERDM_co2.yaml's contract: c_grid_dim 5 (2 constant + 3 remaining
# varying) and scalar_dim 3 (second_of_day, day_of_year, CO₂). The wrapper
# derives c_grid_dim from these two lists and validates scalar_dim, so the
# model and the data pipeline cannot be sized differently.
# (second_of_day, day_of_year, global-mean SST anomaly). Nothing is routed out
# of the grid: the third slot is filled by 12g's rescaler, not by a popped
# channel — which is why scalar_routed_boundary_variables is empty here.
scalar_dim: 3
scalar_routed_boundary_variables: []
# Phase 12b: amip_v2 packing — state [surface | diag | upper_air] with the
# upper-air block level-major (1000 hPa first); c_grid [varying | constant].
channel_layout: v2

# Phase 12f: predicted ocean channels — a tail block of the state axis. Must
# be gridded forcings that actually reach c_grid (so NOT global_mean_co2,
# which is scalar-routed above). The wrapper derives both the tail width and
# the c_grid indices their truth is read from, and hands them to the
# scheduler; nothing here restates a channel number.
#
# ``channel_layout: v2`` is required: its c_grid is [varying | constant], so
# a varying-stream index also addresses the assembled c_grid.
ocean_state_variables:
  - sea_surface_temperature_monthly_interp
  - sea_surface_temperature_anomaly
  - sea_ice_cover_monthly_interp

rolling_dit_kwargs:
  dim: 1024
  num_heads: 16
  temporal_num_heads: 8
  num_blocks: 20
  # W: sizes the forcing cross-attention's temporal tables. MUST equal
  # loss.window_size (erdm_v2.yaml) — the scheduler and the model both need it.
  window_size: 6
  # Legacy-projection widths; unused under input_embed.mode=budget below, kept
  # so switching back to legacy needs no other edit.
  c_grid_embed_dim: 32
  c_scalar_embed_dim: 16

  # Phase 12e: temporal causal cross-attention from the hidden state to the
  # in-window forcing stream, on the last N blocks. Upstream calls this the
  # repo's actual attend-to-the-forcings mechanism (as opposed to the forcings
  # merely contributing input channels). Zero-init gate => no-op at init.
  c_grid_cross_layers: 4
  c_grid_cross_heads: 8

  # Phase 12e: budgeted input projection. Each source gets an explicitly
  # sized, RMS-normalised, disjoint slice of dim, so "how loud are the
  # forcings" is a number here rather than an accident of channel counts
  # (d_state = dim - d_boundary - d_calendar = 640).
  input_embed:
    mode: budget
    d_boundary: 256
    d_calendar: 128
    d_co2: 48            # reserved inside d_calendar, not competing with harmonics
    co2_linear: True     # affine, so scenario CO2 outside the fit range extrapolates
    state_encoder: column   # contract-aware: level-shared upper-air encoder
    d_level: 16
    boundary_encoder: conv2      # strided conv -> GELU -> 3x3 spherical
    boundary_pool_stats: True    # exact cell mean AND std of the raw forcings
    boundary_static_bias: True   # learned geographic field (RoPE is relative)
    source_norm: True

  # Phase 12e: route day-of-year + the trend scalar into the AdaLN vector so
  # they modulate every block, not just add input channels.
  global_cond: True

  # Phase 12e: sigma-conditioned mixture readout. The EDM target is a sigma-
  # and channel-dependent blend of a signal and a noise readout, which one
  # fixed Linear cannot represent; K=2 matches that two-regime structure.
  # (Measured: benchmarks/.../amip_si/RESULTS_projections.md.)
  output_head:
    mode: mix
    num_experts: 2
    # RSI's second readout (the white latent zhat). A separate head instance, so
    # the erdm_fancy checkpoint still warm-starts head 1 one-for-one.
    num_output_heads: 2
    decoder: flat        # flat | column (column mirrors the input encoder)
    d_level: 16
  # Phase 12d option (b): forcings arrive at 1 degree (dataset
  # boundary_zarr_path -> amip_dailyavg_boundary) and this strided conv
  # reduces them to the 45x90 token grid — upstream ERDM_co2.yaml's layout.
  c_grid_downsample: 4
  dropout: 0.0
